[Unit]
Description=Run xcape

[Service]
Type=forking
ExecStart=/usr/bin/xcape -e 'Control_L=Escape'
# Restart=always
# RestartSec=10

[Install]
# WantedBy=default.target
# WantedBy=startup.target
WantedBy=graphical-session.target
